<?php

/**
 * Implements hook_install().
 */
function bricks_bootstrap_install() {
  $entity_manager = \Drupal::entityManager();

  $columns = $entity_manager->getStorage('brick')->create([
    'type' => 'columns',
    'title' => 'Columns',
  ]);
  $columns->save();

  $data = file_get_contents(drupal_get_path('module', 'bricks_bootstrap') . '/content/file/airpods.jpg');
  $file = file_save_data($data, 'public://airpods.jpg', FILE_EXISTS_REPLACE);
  $image = $entity_manager->getStorage('brick')->create([
    'type' => 'image',
    'title' => 'Image',
    'field_image' => [
      'target_id' => $file->id(),
    ],
  ]);
  $image->save();

  $text = $entity_manager->getStorage('brick')->create([
    'type' => 'text',
    'title' => 'Text',
    'field_text' => [
      'value' => 'Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.',
      'format' => 'plain_text',
    ],
  ]);
  $text->save();

  $accordion = $entity_manager->getStorage('brick')->create([
    'type' => 'accordion',
    'title' => 'Accordion',
  ]);
  $accordion->save();

  $carousel = $entity_manager->getStorage('brick')->create([
    'type' => 'carousel',
    'title' => 'Carousel',
  ]);
  $carousel->save();

  $tabs = $entity_manager->getStorage('brick')->create([
    'type' => 'tabs',
    'title' => 'Tabs',
  ]);
  $tabs->save();

  $wrapper = $entity_manager->getStorage('brick')->create([
    'type' => 'wrapper',
    'title' => 'Wrapper',
  ]);
  $wrapper->save();

  $page = $entity_manager->getStorage('node')->create([
    'type' => 'bricky',
    'title' => 'All new AirPods',
    'field_body' => [
      [ 'depth' => 0, 'target_id' => $tabs->id() ],
        [ 'depth' => 1, 'target_id' => $columns->id() ],
          [ 'depth' => 2, 'target_id' => $image->id() ],
          [ 'depth' => 2, 'target_id' => $text->id() ],
        [ 'depth' => 1, 'target_id' => $accordion->id() ],
          [ 'depth' => 2, 'target_id' => $text->id() ],
          [ 'depth' => 2, 'target_id' => $text->id() ],
          [ 'depth' => 2, 'target_id' => $image->id() ],
        [ 'depth' => 1, 'target_id' => $carousel->id() ],
          [ 'depth' => 2, 'target_id' => $image->id() ],
          [ 'depth' => 2, 'target_id' => $image->id() ],
        [ 'depth' => 1, 'target_id' => $text->id() ],
        [ 'depth' => 1, 'target_id' => $image->id() ],
        [ 'depth' => 1, 'target_id' => $wrapper->id(), 'options' => ['css_class' => 'jumbotron'] ],
          [ 'depth' => 2, 'target_id' => $text->id() ],
    ],
  ]);
  $page->save();
}
